import styled from "styled-components";

export const GamesPageContentContainer = styled.section`
  display: flex-column;
  margin: 5em 0;
`;

export const GameImageContainer = styled.div`
  max-width: 30em;
`;

export const GameDescription = styled.p``;
